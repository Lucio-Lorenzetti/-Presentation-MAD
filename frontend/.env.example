# Sólo hace falta en producción, cuando el frontend y el backend viven en
# dominios distintos (ej. frontend en Vercel, backend en Railway/Render).
# En desarrollo, Vite ya redirige /api al backend (ver vite.config.js).
VITE_API_URL=
